---
import Layout from '../layouts/Layout.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'

const title = 'ホーム'
---

<Layout title={title}>
  <Header />

  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-inner">
        <p class="hero-eyebrow">株式会社KIG</p>
        <h1 class="headline-light">中古自動車部品の<br />信頼できるパートナー</h1>
        <p class="subheadline-light">創業以来、品質と誠実さで業界をリード</p>
        <div class="hero-actions">
          <a href="/contact" class="btn-primary">お問い合わせ</a>
          <a href="/business" class="btn-secondary-light">事業内容を見る</a>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="features section">
      <div class="container">
        <h2 class="section-title headline">KIGの特徴</h2>
        <p class="section-sub subheadline">品質・信頼・グローバル。それがKIGです。</p>
        <div class="features-grid">
          <div class="feature-card">
            <div class="feature-icon">📦</div>
            <h3>豊富な在庫</h3>
            <p>取扱部品数業界トップクラス。即日納品対応可能</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">✅</div>
            <h3>品質管理</h3>
            <p>厳格なチェック基準。不良品ゼロを目指す</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🌏</div>
            <h3>グローバル対応</h3>
            <p>世界中への輸出実績。多言語サポート</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta section">
      <div class="container cta-inner">
        <h2 class="headline">新規取引先募集中</h2>
        <p class="subheadline">ご質問・ご相談はお気軽にお問い合わせください</p>
        <a href="/contact" class="btn-primary">今すぐ問い合わせ</a>
      </div>
    </section>
  </main>

  <Footer />
</Layout>

<style>
  /* Hero */
  .hero {
    background-color: #1d1d1f;
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
  }

  .hero-inner {
    padding: 80px 24px;
    max-width: 900px;
    margin: 0 auto;
  }

  .hero-eyebrow {
    font-size: 17px;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.6);
    letter-spacing: 0.02em;
    margin-bottom: 16px;
  }

  .hero-inner h1 {
    margin-bottom: 20px;
  }

  .hero-inner p {
    margin-bottom: 40px;
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-secondary-light {
    display: inline-block;
    color: rgba(255, 255, 255, 0.85);
    font-size: 17px;
    font-weight: 400;
    padding: 12px 24px;
    border-radius: 980px;
    border: 1px solid rgba(255, 255, 255, 0.4);
    transition: all 0.2s ease;
  }

  .btn-secondary-light:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.6);
  }

  /* Features */
  .features {
    background-color: #f5f5f7;
  }

  .section-title {
    text-align: center;
    margin-bottom: 12px;
  }

  .section-sub {
    text-align: center;
    margin-bottom: 60px;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .feature-card {
    background: #ffffff;
    border-radius: 18px;
    padding: 40px 32px;
  }

  .feature-icon {
    font-size: 40px;
    margin-bottom: 20px;
  }

  .feature-card h3 {
    font-size: 21px;
    font-weight: 600;
    color: #1d1d1f;
    margin-bottom: 10px;
    letter-spacing: -0.01em;
  }

  .feature-card p {
    font-size: 15px;
    color: #6e6e73;
    line-height: 1.6;
  }

  /* CTA */
  .cta {
    background-color: #ffffff;
    text-align: center;
  }

  .cta-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
  }

  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
