---
import Layout from '../layouts/Layout.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'

const jobs = [
  {
    title: '営業スタッフ',
    type: '正社員',
    salary: '月給 250,000円〜600,000円',
    salaryNote: '試用期間中：230,000円',
    location: '大阪府箕面市今宮3丁目2-3',
    desc: '自動車パーツの仕入れ営業、チューニングショップや整備工場への訪問営業が中心です。',
    duties: [
      '自動車パーツの仕入れ営業',
      'チューニングショップへの訪問営業',
      '整備工場への訪問営業',
      '既存顧客のフォロー',
    ],
  },
  {
    title: '倉庫内物流スタッフ',
    type: '正社員',
    salary: '月給 230,000円〜450,000円',
    salaryNote: '試用期間中：210,000円',
    location: '大阪府箕面市今宮3丁目2-3',
    desc: 'パーツの検品・洗浄・梱包・写真撮影・発送作業を担当していただきます。',
    duties: [
      'パーツの検品・洗浄',
      '梱包・発送作業',
      '商品写真撮影',
      '在庫管理',
    ],
  },
  {
    title: 'データ入力スタッフ',
    type: '正社員',
    salary: '月給 220,000円〜350,000円',
    salaryNote: '試用期間中：200,000円',
    location: '大阪府箕面市今宮3丁目2-3',
    desc: '商品情報をシステムに入力し、市場調査なども担当していただきます。',
    duties: [
      '商品情報のシステム入力',
      '市場調査・価格リサーチ',
      'データ整理・管理',
      '各種事務作業',
    ],
  },
]
---

<Layout title="採用情報">
  <Header />
  <main>
    <section class="page-hero">
      <div class="container">
        <p class="breadcrumb"><a href="/">HOME</a> / 採用情報</p>
        <h1 class="page-title">採用情報</h1>
        <p class="page-sub">あなたのアイデアを形にする会社</p>
      </div>
    </section>

    <section class="section message-section">
      <div class="container">
        <div class="message-block fade-up">
          <p class="message-main">創造性と好奇心を持ち続けて、<br />情熱的に仕事を心から楽しむ。</p>
          <p class="message-body">株式会社KIGは「人材力のある組織」を目指しています。年功序列制度を見直し、セカンドチャンス体制を導入することで、一人ひとりが能力を最大限に発揮できる環境を整えています。あなたのアイデアを形にする場所がここにあります。</p>
        </div>
        <div class="conditions-grid fade-up">
          <div class="condition-item">
            <p class="cond-icon">📅</p>
            <p class="cond-title">完全週休2日制</p>
            <p>土日休み</p>
          </div>
          <div class="condition-item">
            <p class="cond-icon">🛡</p>
            <p class="cond-title">社会保険完備</p>
            <p>健康・厚生・雇用・労災</p>
          </div>
          <div class="condition-item">
            <p class="cond-icon">🚜</p>
            <p class="cond-title">資格取得支援</p>
            <p>フォークリフト免許取得支援</p>
          </div>
          <div class="condition-item">
            <p class="cond-icon">🔄</p>
            <p class="cond-title">セカンドチャンス</p>
            <p>能力重視の評価制度</p>
          </div>
        </div>
      </div>
    </section>

    <section class="section jobs-section">
      <div class="container">
        <p class="section-label fade-up">OPEN POSITIONS</p>
        <h2 class="section-title fade-up">募集職種</h2>
        <div class="jobs-list">
          {jobs.map((job, i) => (
            <div class={`job-card fade-up ${i > 0 ? `fade-up-delay-${i}` : ''}`}>
              <div class="job-header">
                <div>
                  <span class="job-type">{job.type}</span>
                  <h3 class="job-title">{job.title}</h3>
                </div>
                <div class="job-salary">
                  <p class="salary-num">{job.salary}</p>
                  <p class="salary-note">{job.salaryNote}</p>
                </div>
              </div>
              <p class="job-desc">{job.desc}</p>
              <div class="job-details">
                <div>
                  <p class="detail-label">主な業務</p>
                  <ul>
                    {job.duties.map(d => <li>{d}</li>)}
                  </ul>
                </div>
                <div>
                  <p class="detail-label">勤務地</p>
                  <p class="detail-val">{job.location}</p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <section class="entry-section fade-up">
      <div class="container entry-inner">
        <div>
          <h2>エントリーはこちら</h2>
          <p>ご応募・ご質問はお問い合わせフォームよりお気軽にご連絡ください。</p>
        </div>
        <a href="/contact" class="btn btn-white">エントリーする</a>
      </div>
    </section>
  </main>
  <Footer />
</Layout>

<style>
  .page-hero {
    background: linear-gradient(135deg, #0d1f6e 0%, #1e4fd8 100%);
    padding: 60px 0;
  }
  .breadcrumb { font-size: 12px; color: rgba(255,255,255,0.5); margin-bottom: 16px; }
  .breadcrumb a { color: rgba(255,255,255,0.5); }
  .page-title { color: #fff; margin-bottom: 12px; }
  .page-sub { color: rgba(255,255,255,0.65); font-size: 16px; }

  .message-section { background: #f8f9ff; }
  .message-block {
    text-align: center;
    max-width: 720px;
    margin: 0 auto 56px;
  }
  .message-main {
    font-size: clamp(22px, 3vw, 32px);
    font-weight: 700;
    color: #0d1f6e;
    line-height: 1.5;
    letter-spacing: -0.01em;
    margin-bottom: 24px;
  }
  .message-body {
    font-size: 15px;
    color: #374151;
    line-height: 1.9;
  }
  .conditions-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }
  .condition-item {
    background: #fff;
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    padding: 28px 20px;
    text-align: center;
  }
  .cond-icon { font-size: 32px; margin-bottom: 12px; }
  .cond-title { font-size: 15px; font-weight: 700; color: #111827; margin-bottom: 6px; }
  .condition-item p:last-child { font-size: 13px; color: #6b7280; }

  .jobs-section { background: #fff; }
  .jobs-list { margin-top: 48px; display: flex; flex-direction: column; gap: 24px; }
  .job-card {
    border: 1px solid #e5e7eb;
    border-radius: 16px;
    padding: 40px;
    transition: box-shadow 0.2s, border-color 0.2s;
  }
  .job-card:hover {
    box-shadow: 0 8px 32px rgba(13,31,110,0.1);
    border-color: #0d1f6e;
  }
  .job-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 24px;
    gap: 24px;
  }
  .job-type {
    display: inline-block;
    background: #e8edf8;
    color: #0d1f6e;
    font-size: 12px;
    font-weight: 600;
    padding: 3px 10px;
    border-radius: 4px;
    margin-bottom: 10px;
  }
  .job-title {
    font-size: 24px;
    font-weight: 700;
    color: #111827;
    letter-spacing: -0.01em;
  }
  .job-salary { text-align: right; }
  .salary-num { font-size: 18px; font-weight: 700; color: #0d1f6e; }
  .salary-note { font-size: 12px; color: #9ca3af; margin-top: 4px; }
  .job-desc {
    font-size: 15px;
    color: #374151;
    line-height: 1.7;
    margin-bottom: 28px;
    padding-bottom: 28px;
    border-bottom: 1px solid #e5e7eb;
  }
  .job-details {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 32px;
  }
  .detail-label {
    font-size: 12px;
    font-weight: 600;
    color: #9ca3af;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-bottom: 12px;
  }
  .job-details ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .job-details li {
    font-size: 14px;
    color: #374151;
    padding-left: 14px;
    position: relative;
  }
  .job-details li::before {
    content: '—';
    position: absolute;
    left: 0;
    color: #0d1f6e;
    font-weight: 700;
  }
  .detail-val { font-size: 14px; color: #374151; line-height: 1.6; }

  .entry-section {
    background: linear-gradient(135deg, #0d1f6e 0%, #1e4fd8 100%);
    padding: 80px 0;
  }
  .entry-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 40px;
  }
  .entry-section h2 {
    font-size: 28px;
    font-weight: 700;
    color: #fff;
    margin-bottom: 10px;
  }
  .entry-section p { font-size: 15px; color: rgba(255,255,255,0.65); }
  .btn-white { background: #fff; color: #0d1f6e; font-weight: 600; flex-shrink: 0; }
  .btn-white:hover { background: #f3f4f6; }

  @media (max-width: 768px) {
    .conditions-grid { grid-template-columns: repeat(2, 1fr); }
    .job-header { flex-direction: column; }
    .job-salary { text-align: left; }
    .job-details { grid-template-columns: 1fr; }
    .entry-inner { flex-direction: column; text-align: center; }
  }
</style>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) { entry.target.classList.add('visible'); observer.unobserve(entry.target); }
    })
  }, { threshold: 0.1 })
  document.querySelectorAll('.fade-up').forEach(el => observer.observe(el))
</script>
