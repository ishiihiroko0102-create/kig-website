---
import Layout from '../layouts/Layout.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
---

<Layout title="お問い合わせ">
  <Header />
  <main>
    <section class="page-hero">
      <div class="container">
        <p class="breadcrumb"><a href="/">HOME</a> / お問い合わせ</p>
        <h1 class="page-title">お問い合わせ</h1>
        <p class="page-sub">ご質問・ご相談はお気軽にどうぞ。</p>
      </div>
    </section>

    <section class="section">
      <div class="container contact-layout">

        <div class="contact-info fade-up">
          <h2>お気軽にご連絡ください</h2>
          <p>商品に関するご質問、お取引のご相談、その他なんでもお気軽にお問い合わせください。担当者より2営業日以内にご連絡いたします。</p>

          <div class="info-items">
            <div class="info-item">
              <p class="info-label">電話番号</p>
              <p class="info-val">06-6225-1609</p>
              <p class="info-note">平日 10:00〜18:00</p>
            </div>
            <div class="info-item">
              <p class="info-label">FAX</p>
              <p class="info-val">06-6225-1212</p>
            </div>
            <div class="info-item">
              <p class="info-label">本社所在地</p>
              <p class="info-val">〒550-0002</p>
              <p class="info-note">大阪府大阪市西区江戸堀2丁目1-1 9階</p>
            </div>
            <div class="info-item">
              <p class="info-label">定休日</p>
              <p class="info-val">土・日・祝日</p>
            </div>
          </div>

          <div class="recruit-link">
            <p>採用エントリーはこちら</p>
            <a href="/recruit" class="btn btn-outline-navy" style="margin-top:12px">採用情報を見る</a>
          </div>
        </div>

        <div class="contact-form fade-up fade-up-delay-1">
          <form class="form" name="contact" method="POST" data-netlify="true">
            <div class="form-group">
              <label for="name">氏名 <span class="required">必須</span></label>
              <input type="text" id="name" name="name" placeholder="例：山田 太郎" required />
            </div>
            <div class="form-group">
              <label for="tel">電話番号 <span class="required">必須</span></label>
              <input type="tel" id="tel" name="tel" placeholder="例：06-0000-0000" required />
            </div>
            <div class="form-group">
              <label for="email">E-mailアドレス <span class="required">必須</span></label>
              <input type="email" id="email" name="email" placeholder="例：example@email.com" required />
            </div>
            <div class="form-group">
              <label for="subject">件名 <span class="required">必須</span></label>
              <input type="text" id="subject" name="subject" placeholder="お問い合わせの件名" required />
            </div>
            <div class="form-group">
              <label for="message">内容 <span class="optional">任意</span></label>
              <textarea id="message" name="message" rows="6" placeholder="お問い合わせ内容をご記入ください"></textarea>
            </div>
            <button type="submit" class="btn btn-navy submit-btn">送信する</button>
          </form>
        </div>

      </div>
    </section>
  </main>
  <Footer />
</Layout>

<style>
  .page-hero {
    background: linear-gradient(135deg, #0d1f6e 0%, #1e4fd8 100%);
    padding: 60px 0;
  }
  .breadcrumb { font-size: 12px; color: rgba(255,255,255,0.5); margin-bottom: 16px; }
  .breadcrumb a { color: rgba(255,255,255,0.5); }
  .page-title { color: #fff; margin-bottom: 12px; }
  .page-sub { color: rgba(255,255,255,0.65); font-size: 16px; }

  .contact-layout {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 80px;
    align-items: start;
  }

  .contact-info h2 {
    font-size: 24px;
    font-weight: 700;
    color: #111827;
    margin-bottom: 16px;
    letter-spacing: -0.01em;
  }
  .contact-info > p {
    font-size: 15px;
    color: #6b7280;
    line-height: 1.8;
    margin-bottom: 40px;
  }
  .info-items {
    display: flex;
    flex-direction: column;
    gap: 24px;
    margin-bottom: 40px;
  }
  .info-item {
    border-left: 3px solid #0d1f6e;
    padding-left: 16px;
  }
  .info-label {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #9ca3af;
    margin-bottom: 4px;
  }
  .info-val {
    font-size: 17px;
    font-weight: 700;
    color: #111827;
    margin-bottom: 2px;
  }
  .info-note { font-size: 13px; color: #6b7280; }

  .recruit-link {
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    padding: 24px;
  }
  .recruit-link p { font-size: 14px; color: #374151; font-weight: 500; }

  .contact-form {
    background: #f8f9ff;
    border-radius: 16px;
    padding: 48px;
  }
  .form { display: flex; flex-direction: column; gap: 24px; }
  .form-group { display: flex; flex-direction: column; gap: 8px; }
  label {
    font-size: 14px;
    font-weight: 600;
    color: #374151;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .required {
    background: #0d1f6e;
    color: #fff;
    font-size: 11px;
    padding: 2px 7px;
    border-radius: 3px;
    font-weight: 500;
  }
  .optional {
    background: #e5e7eb;
    color: #6b7280;
    font-size: 11px;
    padding: 2px 7px;
    border-radius: 3px;
    font-weight: 500;
  }
  input, textarea {
    font-family: inherit;
    font-size: 15px;
    color: #111827;
    background: #fff;
    border: 1.5px solid #e5e7eb;
    border-radius: 8px;
    padding: 12px 16px;
    transition: border-color 0.2s, box-shadow 0.2s;
    outline: none;
    width: 100%;
  }
  input:focus, textarea:focus {
    border-color: #0d1f6e;
    box-shadow: 0 0 0 3px rgba(13,31,110,0.08);
  }
  textarea { resize: vertical; line-height: 1.6; }
  .submit-btn { width: 100%; justify-content: center; font-size: 16px; padding: 16px; }

  @media (max-width: 768px) {
    .contact-layout { grid-template-columns: 1fr; }
    .contact-form { padding: 28px 20px; }
  }
</style>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) { entry.target.classList.add('visible'); observer.unobserve(entry.target); }
    })
  }, { threshold: 0.1 })
  document.querySelectorAll('.fade-up').forEach(el => observer.observe(el))
</script>
